name: SonarCloud Analysis Alexandria

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: read

jobs:
  backend:
    name: SonarCloud - Backend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Cache Maven packages
        uses: actions/cache@v5
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('alexandria-backend/**/pom.xml') }}

      - name: Build and analyze (backend)
        working-directory: ./alexandria-backend
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: >
          mvn -B verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar
          -Dsonar.projectKey=mba-puc-alexandria_alexandria-backend
          -Dsonar.organization=mba-puc-alexandria
          -Dsonar.scanner.skipJreProvisioning=true
          -Dsonar.exclusions=**/target/**,**/*.java.orig

  frontend:
    name: SonarCloud - Frontend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: '20'

      - name: Set up JDK 17 for SonarCloud
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install dependencies
        working-directory: ./alexandria-frontend
        run: npm ci

      - name: Run tests
        working-directory: ./alexandria-frontend
        run: npm test

      - name: SonarCloud Scan (frontend)
        uses: SonarSource/sonarqube-scan-action@v6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          projectBaseDir: alexandria-frontend
          args: >
            -Dsonar.host.url=https://sonarcloud.io
            -Dsonar.projectKey=mba-puc-alexandria_alexandria
            -Dsonar.organization=mba-puc-alexandria
            -Dsonar.sources=src
            -Dsonar.javascript.lcov.reportPaths=coverage/lcov.info
            -Dsonar.scanner.skipJreProvisioning=true
            -Dsonar.exclusions=**/target/**,**/*.java.orig
